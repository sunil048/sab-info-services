spring:
  application:
    name: sab-info-services
  config:
    import: "configserver:http://config-server:8888"
  cloud:
    config:
      fail-fast: true
  profiles:
    active: DEV
    #active: STAGE